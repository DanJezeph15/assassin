[project]
name = "airbnb-games-backend"
version = "0.1.0"
description = "Backend API for Airbnb party games"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
target-version = "py312"
line-length = 100
